# Unit Tests (Vitest)

Business logic tests: pricing, availability calculation, room state machine
transitions, tenant-scoping helpers, AI tool input/output contracts. Starts
in M1 alongside the first real domain logic.
