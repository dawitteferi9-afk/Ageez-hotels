# Seed Scripts

This directory will contain the M1 seed script that creates **Ageez Grand Hotel**
as database data (not source code) — a `Hotel` row plus its `RoomType`, `Room`,
service, and policy records, per the ChatGPT/Product Owner M0 decision that
dynamic hotel configuration lives in PostgreSQL.

No seed script exists yet. This is reserved for Milestone M1.
