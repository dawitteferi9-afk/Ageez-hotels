/**
 * Placeholder shell at /management — confirms the Phase 2 auth/route
 * skeleton reaches an authenticated page. Dashboard, Reservations, Rooms,
 * Guests, Services, Reports, and Staff accounts are later M4 phases.
 */
export default function ManagementShellPage() {
  return (
    <section>
      <h1 className="font-display text-2xl text-basalt-950">Management</h1>
      <p className="mt-2 text-sm text-basalt-700">
        Signed in successfully. Dashboard and management modules land in
        later M4 phases.
      </p>
    </section>
  );
}
