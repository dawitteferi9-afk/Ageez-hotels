# Guest UI Components

Presentational and container components for the public website. Scope: M2/M3.
Should consume tenant branding/config via props or a tenant context provider,
never hardcode Ageez Grand Hotel copy/values directly in component code.
